# unginx - unprivileged nginx

A size-optimized Nginx build designed for running as any non-root user, featuring:

 - configus support
 - envcfg support
 - disabled TLS/SSL support, and as a result, HTTP/2 support is also disabled
